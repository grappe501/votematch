# VoteMatch — Netlify static site
# Publishes only `public/`. No secrets or env vars belong in this file.
# Build runs typecheck to keep the repo healthy; the landing page does not use DATABASE_URL.

[build]
  command = "npm ci && npm run typecheck"
  publish = "public"

[build.environment]
  NODE_VERSION = "20"
